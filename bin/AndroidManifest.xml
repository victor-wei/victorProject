<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wh.victorwei"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />
    <uses-permission  android:name="android.permission.INTERNET"/>
     <!-- The following permissions are OPTIONAL. -->

  <!-- Used to adjust the work load depending on the type of network the device is using. -->
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <!-- Used to load images from the gallery content provider. -->
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <!-- Used to load images for contact photos. -->
  <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.TreeViewTest"></activity>
        <activity android:name="com.wh.victorwei.activity.DrawLayoutActivity"></activity>
        <activity android:name="com.wh.victorwei.activity.ImageLoadActivity"></activity>
        <activity android:name="com.wh.victorwei.test.ImageTestActivity"></activity>
    </application>
    

</manifest>
